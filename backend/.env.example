MONGO_URI=
PORT=5011

# Comma-separated allowed frontend origins for CORS


STATEANDCITY_API_KEY=

# Optional — reverse geocode for travel / site-visit addresses (Google preferred, Nominatim fallback)
GOOGLE_MAPS_API_KEY=
